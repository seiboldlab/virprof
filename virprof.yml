userdata:
  virprof:
    version: 0.2.0

include:
  - pipelines.yml
  - references.yml

directories:
  rules:
    virprof:
      rules

conda:
  env_specs:
    - conda_envs

projects:
  test:
    data: test_data/test.csv
