userdata:
  virprof:
    version: 0.7.0

include:
  - pipelines.yml
  - references.yml

directories:
  rules:
    virprof:
      rules

conda:
  env_specs:
    - conda_envs

projects:
  test:
    data: !workdir test_data/test.csv
