userdata:
  virprof:
    version: 0.9.0-dev

include:
  - pipelines.yml
  - references.yml

directories:
  rules:
    virprof:
      rules

conda:
  env_specs:
    - conda_envs

projects:
  test:
    data: !workdir test_data/test.csv
